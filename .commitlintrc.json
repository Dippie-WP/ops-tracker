{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "header-min-length": [2, "always", 10],
    "header-max-length": [2, "always", 72],
    "type-enum": [2, "always", ["feat", "fix", "refactor", "test", "docs", "chore", "perf", "ci", "revert"]]
  }
}
