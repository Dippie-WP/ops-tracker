# Ops Tracker — Environment Variables
# Copy to .env and fill in values

# Server
PORT=3000
NODE_ENV=production

# Database (path to SQLite file, directory must exist and be writable)
DATA_DIR=/data

# External port (used in links/redirects if behind a proxy)
EXTERNAL_PORT=5050
